---
type Props = {
	size: number;
    iconHTML: string;
};
const { size, iconHTML } = Astro.props;
---

<svg width={size} height={size} set:html={iconHTML}>
    <style>
        path, rect {
            fill: var(--theme-text);
        }
    </style>
</svg>